---
upgrade:
  - |
    The ``qiskit-addon-aqc-tensor`` package bundled in the Ray runner image has
    been updated from ``0.2.0`` to ``0.3.0`` (installed with the ``quimb-jax``
    extra). Qiskit Functions that rely on ``qiskit-addon-aqc-tensor`` should
    review the addon's ``0.3.0`` release notes for any API changes.
