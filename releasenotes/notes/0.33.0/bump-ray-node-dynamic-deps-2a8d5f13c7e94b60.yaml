---
upgrade:
  - |
    Several dependencies bundled in the Ray runner image have been updated:

    * ``pyscf`` from ``2.11.0`` to ``2.13.1``
    * ``qiskit-addon-sqd`` from ``0.12.0`` to ``0.12.1``

    Qiskit Functions that rely on these packages should review their respective
    release notes for any API changes.
