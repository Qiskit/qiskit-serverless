---
features:
  - |
    :class:`.IBMServerlessClient`  now supports custom gateway host URLs through the ``host`` parameter.
    This allows for a more streamlined testing of the client connecting to non-production gateways.